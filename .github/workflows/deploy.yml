name: Deploy to Lambda

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies into package directory
        # ably added: status_publisher.py imports AblyRest at runtime
        run: pip install boto3 openai ably --target ./package

      - name: Build zip
        run: |
          cd package
          zip -r ../lambda.zip .
          cd ..
          # status_publisher.py added: it is imported by handler at runtime
          zip lambda.zip handler.py status_publisher.py

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload to Lambda
        run: |
          aws lambda update-function-code \
            --function-name ${{ secrets.LAMBDA_FUNCTION_NAME }} \
            --zip-file fileb://lambda.zip
